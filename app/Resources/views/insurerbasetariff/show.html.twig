{% extends 'base.html.twig' %}

{% block body %}
    <h1>InsurerBaseTariff</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insurerBaseTariff.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ insurerBaseTariff.amount }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('insurerbasetariff_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('insurerbasetariff_edit', { 'id': insurerBaseTariff.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
