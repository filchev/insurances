{% extends 'base.html.twig' %}

{% block body %}
    <h1>CarCategoryTariff</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carCategoryTariff.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cms_carcategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cms_carcategory_edit', { 'id': carCategoryTariff.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
